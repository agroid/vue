
// import Vue from 'vue'
import EmbrapaButton from './Button.vue'

const components = {
  EmbrapaButton
}
/*
Object.keys(components).forEach(name => {
  // console.log(JSON.stringify(name))
  Vue.component(name, components[name])
})
*/
export default components
